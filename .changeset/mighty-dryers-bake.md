---
'@raxium/vue': patch
'@raxium/themes': patch
---

Honor `bordered` theme variant on overlay crafts (`dialog` / `menu` / `popover` / `hover-card` / `select` / `date-picker`); fix missing `ThemeProvider` import in Slider tooltip thumbs.
