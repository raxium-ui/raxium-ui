---
description: 新建或编辑 .changeset/*.md 时，根据当前改动自动补全简明 changelog
globs: .changeset/*.md
alwaysApply: false
---

# Changeset changelog

> **说明**
> - **终端**：`pnpm changeset:fill` 会优先调用 standalone **`agent` CLI**（`agent -p --force`）自动填写；需先安装 Cursor CLI（Windows: `irm 'https://cursor.com/install?win32=true' | iex`）。未安装时回退为 git draft。
> - **IDE**：Agent Chat 会话 + `.cursor/hooks.json`（`stop` / `afterFileEdit` / `postToolUse`）对占位/Draft 正文发出 follow-up。
> - **手动**：`/changeset-changelog` 或 `@` changeset 文件。

当打开、新建或编辑 `.changeset/*.md`（排除配置说明文件）时：

1. 若正文为空、为占位（如 `changelog` / `change log` / `TODO`），或用户要求写 changelog：根据当前 `git status` / `git diff` 生成 **约 2～3 句英文**，写清用户可感知的 what/why，写回该文件第二个 `---` 之后。避免电报式一句带过。
2. **保留** frontmatter 中的包名与 `patch` / `minor` / `major`，除非与改动明显不符且用户同意修改。
3. 遵循 `.cursor/agents/changeset-changelog.md` 的流程与文风。
4. 不要为此自动 commit。
