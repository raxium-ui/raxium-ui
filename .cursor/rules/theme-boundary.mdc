---
description: Vue 组件 theme / craft 边界：tokens、crafts 分层，root vs structural 用法
globs: "packages/vue/**/*.{vue,ts,tsx}"
alwaysApply: false
---

# Theme 边界约束

## 类型分层（`@raxium/themes/runtime`）

| 类型 | 形状 | 用途 |
| --- | --- | --- |
| `ThemeProps` | tokens only（`skin` / `surface` / `size` / …） | `:theme`、Scope Theme、`ThemeProvider` |
| `ThemeConfig` | `ThemeProps` + optional `crafts?` | **仅** `RUIConfig.theme` |
| `ResolvedTheme` | `ThemeProps` + required `crafts` | `useTheme` 产出 / Component Theme |

- **不要**在 `ThemeProps` / Scope / 组件 `:theme` 上挂 `crafts`。
- Props mixin：`ThemeCrafts<'tvXxx'>`（`theme` + 单 key `craft`）；结构子件用 `ThemeNoCrafts`（仅 `theme`）。
- `ThemeCrafts` **只绑一个主 craft key**（禁止 `'tvA' | 'tvB'` union）。内嵌原语（如 Menu 内 checkbox、TagsInput 内 input）改结构走应用级 `RUIConfig.theme.crafts`。

## Crafts 两层

1. **应用级**：`RUIConfig.theme.crafts`（preset / 全局）← `useTheme` 只从这里 merge crafts。
2. **实例级**：根组件 `:craft`（`CraftOverride`）
   - 叶子：`useCraft(theme, 'tvXxx', variants, () => craft)`
   - 复合根（provide 给子件）：`useThemeCraft(theme, 'tvXxx', () => craft)` 再 `useProvideComponentTheme(themed, …)`

## Root 组件（独立边界）

- `useTheme(...)` 解析 tokens 全链路（defaults → config → component-config → Scope → props）。
- 有 `:craft` 时必须 **bake**（`useThemeCraft`），再 `useProvideComponentTheme(themed, () => propsTheme)`；勿把未 bake 的 theme provide 出去。
- 解构时剥离 `theme` / `craft`，勿 forward 进 Ark / 第三方 props。
- 典型：`Dialog`、`Tooltip`、`Popover`、`Menu`、`Select`、`DatePicker`、`Messager`、`Toaster`。

## Structural 组件（内部结构）

- `useInheritedTheme(...)` 继承父组件已解析的 `ResolvedTheme`（含 bake 后的 crafts）。
- 若还承载子节点（`*Content` / Arrow 等），用 `useProvideStructuralComponentTheme(...)` 向下传 Component Theme，**不得**泄漏到 Scope Theme。
- **不要**在 structural 中使用 `useTheme` 或 `useProvideComponentTheme`。
- 无根级 `:craft`（`ThemeNoCrafts`）；局部样式用 `ui` / `class`。

## 通道语义

- **Scope Theme**（`ThemeProvider` / 显式 `:theme` provide）：**tokens only**；供独立后代的 `useTheme` 读取。
- **Component Theme**：`ResolvedTheme`；供结构子件 `useInheritedTheme`。

## 快速判断

- 可单独使用、可配 `RUIConfig.xxx.theme`（tokens）→ root。
- 仅作为某 root 内部部件（`*Content/*Item/*Trigger/*Header/*Footer`）→ structural。

## 例外

- 命名像 structural 但语义是 root：PR 说明原因，并更新本规则或 ESLint。
